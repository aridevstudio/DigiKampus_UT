{{-- REMOVED — E-Library AlpineJS catalog. Launcher langsung redirect ke URL
     yang dikonfigurasi admin via kolom app_registry_overrides.external_url
     (fallback: AppRegistryService::CATALOG). --}}
